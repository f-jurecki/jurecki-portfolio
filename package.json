{
  "name": "jurecki-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check:solitaire": "node scripts/check-solitaire.mjs",
		"build": "astro build",
		"check:case-modules": "node scripts/check-case-modules.mjs",
		"test:responsive": "npm run build && node scripts/check-responsive.mjs",
		"preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.9"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
